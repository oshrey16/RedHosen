pipeline {
    agent any
    environment {
        flutter = "/flutterSDK/flutter/bin/flutter"
    }
    stages {
        stage('Build') {
            steps {
                sh '$flutter test'
            }
        }
    }
}